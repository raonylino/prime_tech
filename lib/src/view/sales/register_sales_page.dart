import 'package:flutter/material.dart';

class RegisterSalesPage extends StatelessWidget {

  const RegisterSalesPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Vendas'),),
           body: Container(),
       );
  }
}